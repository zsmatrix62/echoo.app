import * as React from 'react';
import {Layout} from "@douyinfe/semi-ui";

export const Base64Serde = () => {
    return (
        <Layout>
            <Layout.Header> </Layout.Header>
            <Layout.Content> Base64 Encode/Decode </Layout.Content>
        </Layout>
    );
};